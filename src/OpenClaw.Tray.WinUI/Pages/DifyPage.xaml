<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClawTray.Pages.DifyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- NOTE: 文案暂硬编码中文（企业内部功能）。 -->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 配置区 -->
        <StackPanel Grid.Row="0" Padding="24,16,24,8" Spacing="8" HorizontalAlignment="Stretch" MaxWidth="1000">
            <TextBlock Text="Dify 知识库" Style="{StaticResource TitleTextBlockStyle}"/>
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="12">
                <StackPanel Spacing="8">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBox x:Name="BaseUrlBox" Header="服务地址" Width="360" PlaceholderText="https://dify.example.com"/>
                        <PasswordBox x:Name="ApiKeyBox" Header="API Key" Width="320" PlaceholderText="app-xxxx"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="保存" Click="OnSaveConfigClick"/>
                        <Button Content="测试连接" Click="OnTestClick"/>
                        <ProgressRing x:Name="TestProgress" IsActive="False" Width="18" Height="18"/>
                        <TextBlock x:Name="TestResultText" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- 消息区 -->
        <ListView x:Name="MessagesList" Grid.Row="1" Padding="24,4" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="0,6" Spacing="4" MaxWidth="760">
                        <TextBlock Text="{Binding RoleLabel}" FontWeight="SemiBold"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Text="{Binding Text}" TextWrapping="Wrap" IsTextSelectionEnabled="True"/>
                        <ProgressRing IsActive="{Binding IsStreaming}" Width="14" Height="14" Visibility="{Binding StreamingVisibility}"/>
                        <ItemsControl ItemsSource="{Binding Citations}" Margin="0,4,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                                            CornerRadius="4" Padding="6" Margin="0,2">
                                        <StackPanel Spacing="2">
                                            <TextBlock Text="{Binding DocumentName}" Style="{StaticResource CaptionTextBlockStyle}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Content}" TextWrapping="Wrap" MaxLines="4"
                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- 输入区 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Padding="24,8">
            <TextBox x:Name="InputBox" Width="640" PlaceholderText="向 Dify 知识库提问…" KeyDown="OnInputKeyDown"/>
            <Button x:Name="SendButton" Content="发送" Click="OnSendClick"/>
            <ProgressRing x:Name="SendProgress" IsActive="False" Width="18" Height="18"/>
        </StackPanel>
    </Grid>
</Page>
