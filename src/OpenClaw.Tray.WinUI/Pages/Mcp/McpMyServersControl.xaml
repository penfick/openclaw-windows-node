<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="OpenClawTray.Pages.McpMyServersControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="24,0,24,24" Spacing="16" HorizontalAlignment="Stretch" MaxWidth="1000">

            <StackPanel Orientation="Horizontal" Spacing="8">
                <ProgressRing x:Name="LoadingProgress" IsActive="False" Width="20" Height="20"/>
                <TextBlock x:Name="StatusText" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" TextWrapping="Wrap"/>
            </StackPanel>

            <TextBlock Text="已配置的服务器" Style="{StaticResource BodyStrongTextBlockStyle}"/>
            <ListView x:Name="ServersList" SelectionMode="None" IsItemClickEnabled="False">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,2">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="3">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{Binding Name}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        <TextBlock Text="{Binding Transport}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Display}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="Wrap"/>
                                </StackPanel>
                                <Button Grid.Column="1" Content="删除" Click="OnDeleteClick" Tag="{Binding Name}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <TextBlock x:Name="EmptyHint" Text="尚无 MCP 服务器。" Visibility="Collapsed"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel Spacing="8">
                    <TextBlock Text="添加服务器" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBox x:Name="NameBox" Header="名称" Width="180" PlaceholderText="my-server"/>
                        <ComboBox x:Name="TransportCombo" Header="类型" Width="180" SelectedIndex="0" SelectionChanged="OnTransportChanged">
                            <x:String>stdio</x:String>
                            <x:String>streamable-http</x:String>
                        </ComboBox>
                    </StackPanel>
                    <TextBox x:Name="CommandBox" Header="命令 (stdio，如 npx)" Width="500" PlaceholderText="npx"/>
                    <TextBox x:Name="ArgsBox" Header="参数 (stdio，空格分隔)" Width="600" PlaceholderText="-y @modelcontextprotocol/server-filesystem /tmp"/>
                    <TextBox x:Name="UrlBox" Header="URL (streamable-http)" Width="600" Visibility="Collapsed" PlaceholderText="http://localhost:8080/mcp"/>
                    <Button Content="添加" Click="OnAddClick"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
